export default defineAppConfig({
  ui: {
    header: {
      slots: {
        root: "bg-primary/80 color-white",
      },
    },
    navigationMenu: {
      slots: {
        label: "",
      },
    },
  },
});
